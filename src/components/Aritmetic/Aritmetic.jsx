import Add from "./Add/Add"
import Subtraction from "./Subtraction/Subtraction"

const Aritmetic = () => {
  return (
    <section>
      <Add />
      <Subtraction />
    </section>
  )
}

export default Aritmetic